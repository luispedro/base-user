# These are some basic packages for a system

# We install vim-gtk to get +xterm_clipboard
sudo apt-get install \
    vim-gtk \
    zsh \
    tofrodos \
    git \
    gawk \
    ccache

sudo apt-get install texlive
